/* tslint:disable */
/* eslint-disable */
/**
 * InvoicePDFs
 * InvoicePDFs turns a JSON document into a polished PDF — invoices, credit notes, quotes, receipts, proformas, purchase orders and delivery notes. The API validates your data, computes totals correctly (tax, discounts, shipping), and returns a downloadable, professional PDF.  This package is the official client for the InvoicePDFs API, generated from the OpenAPI specification.  Documentation: https://invoicepdfs.com
 *
 * The version of the OpenAPI document: 0.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { CodeOut } from './CodeOut';
import {
    CodeOutFromJSON,
    CodeOutFromJSONTyped,
    CodeOutToJSON,
} from './CodeOut';

/**
 * A coded list from a standard, and whether it is the whole of one.
 * 
 * `exhaustive` is the field that changes what a client does. `true` means a
 * value outside `data` is wrong, so the list can back a picker with no escape
 * hatch. `false` means `data` is a shortlist of the codes an invoice usually
 * needs — the API accepts any code, nothing validates against this, and
 * treating it as closed rejects values that are perfectly valid.
 * @export
 * @interface CodeListResponse
 */
export interface CodeListResponse {
    /**
     * 
     * @type {Array<CodeOut>}
     * @memberof CodeListResponse
     */
    data: Array<CodeOut>;
    /**
     * The code list these values come from.
     * @type {string}
     * @memberof CodeListResponse
     */
    standard: string;
    /**
     * Whether `data` is the complete list. When false it is a shortlist and other codes remain valid.
     * @type {boolean}
     * @memberof CodeListResponse
     */
    exhaustive: boolean;
}

/**
 * Check if a given object implements the CodeListResponse interface.
 */
export function instanceOfCodeListResponse(value: object): value is CodeListResponse {
    if (!('data' in value) || value['data'] === undefined) return false;
    if (!('standard' in value) || value['standard'] === undefined) return false;
    if (!('exhaustive' in value) || value['exhaustive'] === undefined) return false;
    return true;
}

export function CodeListResponseFromJSON(json: any): CodeListResponse {
    return CodeListResponseFromJSONTyped(json, false);
}

export function CodeListResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): CodeListResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'data': ((json['data'] as Array<any>).map(CodeOutFromJSON)),
        'standard': json['standard'],
        'exhaustive': json['exhaustive'],
    };
}

export function CodeListResponseToJSON(value?: CodeListResponse | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'data': ((value['data'] as Array<any>).map(CodeOutToJSON)),
        'standard': value['standard'],
        'exhaustive': value['exhaustive'],
    };
}

